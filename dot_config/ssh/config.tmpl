# =============================================================================
# SSH Config (Shared Settings) - managed by chezmoi
# =============================================================================
# This file is included by ~/.ssh/config and contains shared settings.
# Machine-specific hosts and auto-generated blocks stay in ~/.ssh/config.

# -----------------------------------------------------------------------------
# Default settings for all hosts
# -----------------------------------------------------------------------------
Host *
    # Forward SSH agent to remote machines
    ForwardAgent yes

    # Keep connections alive
    ServerAliveInterval 60
    ServerAliveCountMax 3

{{- if .has1Password }}
    # Use 1Password SSH agent (macOS)
    IdentityAgent "~/Library/Group Containers/2BUA8C4S2C.com.1password/t/agent.sock"
{{- end }}

    # Security
    HashKnownHosts no
    AddKeysToAgent yes

    # Performance
    Compression no

# -----------------------------------------------------------------------------
# Common hosts
# -----------------------------------------------------------------------------
# GitHub
Host github.com
    HostName github.com
    User git
